allowBuilds:
  '@firebase/util': true
  esbuild: true
  protobufjs: true
# ultimate-express depends on uWebSockets.js, which is only distributed via
# GitHub (not published to the npm registry), so it appears as a git-sourced
# transitive dependency. Allow that.
blockExoticSubdeps: false
